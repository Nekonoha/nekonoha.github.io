{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "nuxt cleanup",
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "start": "nuxt start",
    "sync:releases": "node scripts/sync-releases.mjs",
    "sync:illustrations": "node scripts/sync-illustrations.mjs",
    "sync:content": "npm run sync:releases && npm run sync:illustrations",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@splidejs/splide": "^4.1.4",
    "@splidejs/vue-splide": "^0.6.12",
    "nuxt": "^3.10.0"
  }
}
